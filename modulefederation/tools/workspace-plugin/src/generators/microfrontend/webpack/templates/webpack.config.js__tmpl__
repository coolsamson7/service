const { withModuleFederation } = require('@nx/angular/module-federation');

module.exports = withModuleFederation({
   name: '<%- projectName %>',
   exposes: {
      './Module': '<%= moduleFile %>'
   }
});
