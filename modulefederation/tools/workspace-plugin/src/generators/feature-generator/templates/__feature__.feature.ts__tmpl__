import { Component, Injector } from '@angular/core';
import { AbstractFeature, Feature } from "@modulefederation/portal";

@Feature({
    id: 'home'
})
@Component({
    standalone: true,
    selector: '<%= selector %>',
    templateUrl: '<%= name %>.feature.html',
    styleUrls: ['<%= name %>.feature.<%= style %>'],
})
export class <%= feature %>Component extends AbstractFeature {
    // constructor

    constructor(injector: Injector) {
        super(injector);
    }
}
